# Omni — Free AI Chat Bot

A free, multi-model AI chat studio. Talk to **real** Claude Fable, GPT, Grok, Gemini, Kimi, and DeepSeek models — not simulated labels — with **no API key**.

Powered by [Puter.js](https://developer.puter.com) (user-pays: sign in with a free Puter account).

## Live

**https://coolkidhacks.github.io/FREE-AI-CHAT-BOT/**

## Features

- Real streaming replies via `puter.ai.chat()` (each model ID is a real Puter-hosted model)
- Model switcher: Claude Fable 5.1 / 5, Opus, Sonnet, GPT-5.6 Luna, GPT-4o, Grok 4.6, Gemini, Kimi K3, DeepSeek — plus extra models loaded live from Puter
- Thinking traces (toggle) and work effort (low → max)
- Multi-chat sidebar, local history, search, delete
- Image attach, stop generation, copy, regenerate
- Dark studio UI, mobile layout

## How to use

1. Open the site.
2. Click **Sign in free** (Puter popup — required so the model can run). Allow popups if the browser asks.
3. Pick a model and effort.
4. Type a message and press Enter.

Chats stay in this browser (`localStorage`). Nothing is stored on a server we control.

## Deploy your own

This is a single-file app:

1. Fork or copy `index.html`.
2. Enable GitHub Pages on the repo (branch `main`, root).
3. Wait a minute for the site to go live.

## Credit

AI runs through [Puter.js](https://developer.puter.com).
